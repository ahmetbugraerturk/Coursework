import ps3a

def encrypt_files(input_path, s, output_path):
    """Function to encrypt the message in the input file and write the encrypted message to the output file."""
    ps3a.decrypt_the_message(input_path, -s, output_path)